import InputFieldDropdown from "./Dropdown";

export default {
  title: "InputFieldDropdown",
  component: InputFieldDropdown,
};

const Template = (args) => <InputFieldDropdown {...args} />;

export const Primary = Template.bind({});
